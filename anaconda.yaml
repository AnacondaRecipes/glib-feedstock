anaconda_config_version: 1
upstream_sources:
  - github:
      identifier: GNOME/glib
      use_max_tag: true
      include_regex: '^2\.\d*[02468]\.\d+$'
    packages:
      - glib
      - glib-tools
      - libglib
